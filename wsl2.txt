docker run -v /mnt/c/app:/app --rm debian 'echo abc > /app/abc.txt'

docker run -it -v /mnt/c/app:/app --rm debian /bin/bash

docker run -it -v /mnt:/app --rm debian /bin/bash -c 'ls /app'

docker volume create --name postgres_data -o type=none -o device=D:\OS Image -o o=bind

docker run -it -v postgres_data:/app --rm debian /bin/bash -c 'ls /app'

docker run -it -v "$(pwd):/app" --rm debian /bin/bash -c 'ls /app'

----------------------

Add user

net localgroup docker-users "your-user-id" /ADD

New-LocalGroup -Name 'docker-users' -Description 'docker Users Group'

Add-LocalGroupMember -Group 'Administrators' -Member ('docker-users') –Verbose

Add-LocalGroupMember -Group 'docker-users' -Member ('Username','Administrators') –Verbose
